package com.sangusantri.app.core.designsystem.theme

import androidx.compose.ui.unit.dp

/** Spacing scale for layout — use instead of ad hoc `.dp` literals per screen (DESIGN_SYSTEM.md). */
object SanguSantriSpacing {
    val extraSmall = 4.dp
    val small = 8.dp
    val medium = 12.dp
    val default = 16.dp
    val large = 24.dp
    val extraLarge = 32.dp
}
