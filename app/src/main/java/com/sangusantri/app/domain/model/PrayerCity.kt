package com.sangusantri.app.domain.model

/** One kabupaten/kota the prayer schedule can be keyed by (myquran `/sholat/kabkota`). */
data class PrayerCity(
    val id: String,
    val name: String,
)
