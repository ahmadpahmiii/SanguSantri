package com.sangusantri.app.domain.model

/** Al-Qur'an reading-surface appearance (QUR-FR-015 amendment, ADR 0016). Independent of the
 * app/system theme outside Quran — [DARK] is the default, matching the feature's original
 * dark-only baseline. */
enum class QuranThemeMode {
    LIGHT,
    DARK,
}
