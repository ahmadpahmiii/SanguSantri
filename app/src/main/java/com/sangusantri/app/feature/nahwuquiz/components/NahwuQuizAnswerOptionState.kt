package com.sangusantri.app.feature.nahwuquiz.components

/** `Default`/`Selected`/`Correct`/`Incorrect` — design spec states 5-8. [CORRECT]/[INCORRECT] pair
 * a colour change with an icon (never colour alone, per the spec's own accessibility note). */
enum class NahwuQuizAnswerOptionState {
    DEFAULT,
    SELECTED,
    CORRECT,
    INCORRECT,
}
